"alert" ("rerecz") стандартное команда дл вывода диалогового окна

 let age = prompt("Ваш возраст?"); 
    alert("Ваш возраст " + age);

  " prompt "-  Диалоговое окно для ввода информации в поле, используется в переменной


   if - условие
   () - что мы хотим проверить
   {} - что хотим выполнить
   else - иначе, выводим для создания условия


   При помощи функции confirm() можно вывести на экран пользователя модальное окно 
   с текстом и кнопками «Ок» и «Отмена».

Из-за того, что окно модальное — работа с интерфейсом браузера и страницами будет 
заблокирована. Это неудобно, плюс может восприниматься пользователем как попытка 
ограничивать его свободу. Модальное окно для пользователя — окно, которое блокирует 
работу пользователя с браузером до тех пор, пока пользователь это окно не закроет.

 if(confirm("Удалить что-то")){
        alert("удалено")
    }else {
        alert('отменено')
    }

    также можно записать в 1 строку, своего рода рефакторинг

   confirm("Удалить ваш аккаунт?") ? alert("Удалено") : alert("Отменено"); - для нескольнких условий


    confirm("Удалить ваш аккаунт?) : alert ("Удалено") - для одного действия


условие для работы определенный условий с помощью операторов сравнения
    let age = prompt("Ваш возраст?");
    if (age < 18){
        alert("Ваш возраст меньше 18")
    } else if (age > 18) {
        alert("Вам больше 18");
    } else if( age = 18){
        alert("Ваш возраст 18");
    }


Конструкция "switch"
Конструкция switch заменяет собой сразу несколько if.

Она представляет собой более наглядный способ сравнить выражение сразу с несколькими вариантами.

Синтаксис
Конструкция switch имеет один или более блок case и необязательный блок default.

Выглядит она так:

switch(x) {
  case 'value1':  // if (x === 'value1')
    ... 
    [break]

  case 'value2':  // if (x === 'value2')
    ...
    [break]

  default:
    ...
    [break]
}


    Качественей контроль выполняемых кейсов
    
   let age = prompt("Ваш возраст?");
        switch(true){
            case age > 18:
                alert("Вам больше 18");
            break;
            case age < 18:
                alert("Вам меньше 18");
            break;
            default: 
                alert("Ваш возраст 18");
        }

