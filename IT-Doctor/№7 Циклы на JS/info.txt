while (условие){
    действие
}


   let count = 0;
    while (count<10){
        console.log(count);
        count++; сделали цикл на 10 повторений
    }

        let count = 0;
    while (count<10){
        console.log(count);
        if(count==0){  сравнение для равенства
            break; завершаем цикл на нуле
        }
        count++; - ни в коем случае не отрубать
    }


      let count = 5;
    do { выводится значение заданное в цикле, и такое количество раз цикл производит свой расчёт
        console.log(count);
    } while(count>7) 

    for - помогает выполнить определённое количество действий

    for(начальное значение; условие проверки; изменение i дл его завершения){
        выводим наши условия
    }


      for (let i = 0; i < 10; i++) {
        console.log(i);
        выполняется тоже действие, что при первых 2х циклах
    }

    i+=2 - шаг увеличения, выводит нечетные числа

    Циклы в основном применяются для работы с массивами и объектами


     let arr= [5,6,7,8];
    for (let i = 0; i < arr.length; i++) { - перебрали длину массива
        console.log(arr[i]*2); - перебрали каждый индекс и удвоили
        
    }

let arr= [5,6,7,8];
        for (let item of arr ){    на каждой итерации цикла равна элементу массива
            console.log(item);
        }



for in - работа с объектами 
 let obj ={
            'name' : 'arr',
            'age': '20',
            'number': '5555555'
        }

        for(let key in obj){
            console.log(obj[key]); выводим значение ключей
            console.log(key); выводим ключи объекта
        }